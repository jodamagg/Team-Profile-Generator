// module.exports = generateMarkdown;

// addManager()

// const managerCard = manager => {
//     return `<div class="some bootstrap">${manager.getName()}</div>`
// }

// function generateMarkdown(teamMembers) {
//     return `${teamMembers[0].managerName}
// }
